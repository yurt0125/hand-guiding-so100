# Video Assets

Put your experiment videos here and keep these filenames so the webpage can render them directly:

- `value_overlay_demo.mp4`
  - Suggested content: output from `outputs/value_infer/<RUN_NAME>/value/viz/` with value curve + ACP tag overlay.
- `policy_final_rollout.mp4`
  - Suggested content: final policy execution on target task (best run).
- `intervention_compare.mp4`
  - Suggested content: human intervention vs autonomous recovery comparison.

If a file is missing, the page still works and shows the poster image.
